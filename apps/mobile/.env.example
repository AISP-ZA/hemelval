# Kelder environment — copy to .env.local and fill in.
# These are read by expo-constants (EXPO_PUBLIC_* prefix = client-safe, anon only).
EXPO_PUBLIC_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=YOUR-ANON-KEY
