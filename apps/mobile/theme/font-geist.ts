/**
 * GeistMono font path for expo-font loading.
 * The TTF is bundled at theme/assets/GeistMono-Regular.ttf
 * expo-font resolves the require() to a proper asset URI on both web and native.
 */
export const GeistMono = require('./assets/GeistMono-Regular.ttf');
